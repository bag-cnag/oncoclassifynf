{
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "$id": "https://raw.githubusercontent.com/bag-cnag/oncoclassify-annotate/master/assets/schema_input.json",
    "title": "bag-cnag/oncoclassify-annotate pipeline - params.input schema",
    "description": "Schema for the file provided with params.input",
    "type": "array",
    "items": {
        "type": "object",
        "properties": {
            "sample": {
                "type": "string",
                "pattern": "^\\S+$",
                "errorMessage": "Sample name must be provided and cannot contain spaces",
                "meta": ["id"]
            },
            "vcf": {
                "type": "string",
                "format": "file-path",
                "exists": true,
                "pattern": "^([\\S\\s]*\\/)?[^\\s\\/]+\\.vcf\\.gz$",
                "errorMessage": "VCF file for annotation, cannot contain spaces and must have extension '.vcf.gz'"
            },
            "tbi": {
                "type": "string",
                "format": "file-path",
                "exists": true,
                "pattern": "^([\\S\\s]*\\/)?[^\\s\\/]+.tbi$",
                "errorMessage": "VCF file for annotation, cannot contain spaces and must have extension '.tbi'"
            }
        },
        "required": ["sample", "vcf", "tbi"]
    }
}